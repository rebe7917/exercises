"use strict";

const name = `Peterjakob`;
const len = name.length;

console.log(`${name} is ${len} characters long`); 

const letter = name[0];
console.log(`the first letter of ${name} is ${letter}`);
